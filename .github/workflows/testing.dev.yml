name: Docker Image CI

on:
    pull_request:
        branches: ["master"]

jobs:
    deploy:
        runs-on: deploy
        steps:
            - name: Install dependencies
              run: echo "code pushed"

            # - name: Build
            #   run: npx lerna run build